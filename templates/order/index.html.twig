{% extends 'base.html.twig' %}

{% block title %}Hello CategoryController!{% endblock %}

{% block body %}

  <div class="container my-5 small-container">
    <h1>Ma commande</h1>
    {{ form_start(deliveryForm) }}
      {{ form_row(deliveryForm.addresses)}}
      <a href="{{path('app_account_addresses_form')}}" class="small">Ajouter une nouvelle addresse de livraison</a>

      <hr class="my-4">
      {{ form_row(deliveryForm.carriers)}}
    {{ form_end(deliveryForm) }}
  </div>

{% endblock %}
