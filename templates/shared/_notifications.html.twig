<div class="container">
  {% for label, messages in app.flashes %}
      {% for message in messages %}
          <div class="mt-4 alert alert-{{ label }}">
              {{ message }}
          </div>
      {% endfor %}
  {% endfor %}
</div>